let baseUrl = "http://127.0.0.1:7001";
const service = {
  index: baseUrl + "/default/index",
  details: baseUrl + "/default/details/",
  navbar: baseUrl + "/default/navbar",
  list: baseUrl + "/default/list/"
};

export default service;
